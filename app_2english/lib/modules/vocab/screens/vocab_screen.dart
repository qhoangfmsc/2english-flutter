import 'package:flutter/material.dart';

class VocabScreen extends StatelessWidget {
  const VocabScreen({super.key});

  // ========== Build Methods ==========
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Your Vocab'));
  }
}
